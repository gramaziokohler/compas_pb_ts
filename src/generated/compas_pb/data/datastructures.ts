// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.34.0
// source: compas_pb/data/datastructures.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AnyData } from "./message";

export const protobufPackage = "compas_pb.data";

/**
 * A single vertex attribute stored column-wise across vertices.
 * If `indices` is empty the column is dense: one value per vertex, in vertex order.
 * Otherwise `indices[k]` is the vertex index carrying the k-th value (sparse).
 * Exactly one of the typed arrays / `values` is populated, selected by `kind`.
 */
export interface AttributeColumn {
  name: string;
  indices: number[];
  /** 0 = double, 1 = int, 2 = bool, 3 = generic AnyData */
  kind: number;
  doubles: number[];
  ints: number[];
  bools: boolean[];
  /** mixed / non-numeric columns */
  values: AnyData[];
}

export interface MeshData {
  guid?: string | undefined;
  name?: string | undefined;
  /** flat coordinate array: 3 doubles (x, y, z) per vertex */
  vertices: number[];
  /**
   * Faces in CSR form: all face vertex indices concatenated, with face_sizes giving the
   * vertex count of each face (so face i spans the next face_sizes[i] entries).
   */
  faceVertices: number[];
  faceSizes: number[];
  attributes: { [key: string]: AnyData };
  /** vertex attrs, column-wise (by vertex index) */
  vertexAttributeColumns: AttributeColumn[];
  /** face attrs, column-wise (by face index) */
  faceAttributeColumns: AttributeColumn[];
  /** edge attrs, column-wise (aligned to edge_keys) */
  edgeAttributeColumns: AttributeColumn[];
  /** keys of edges carrying attributes */
  edgeKeys: AnyData[];
  defaultVertexAttributes: { [key: string]: AnyData };
  defaultFaceAttributes: { [key: string]: AnyData };
  defaultEdgeAttributes: { [key: string]: AnyData };
}

export interface MeshData_AttributesEntry {
  key: string;
  value: AnyData | undefined;
}

export interface MeshData_DefaultVertexAttributesEntry {
  key: string;
  value: AnyData | undefined;
}

export interface MeshData_DefaultFaceAttributesEntry {
  key: string;
  value: AnyData | undefined;
}

export interface MeshData_DefaultEdgeAttributesEntry {
  key: string;
  value: AnyData | undefined;
}

export interface FaceData {
  /** indices into the vertices array */
  vertexIndices: number[];
}

export interface PolyhedronData {
  guid?: string | undefined;
  name?: string | undefined;
  /** flat x,y,z triplets */
  vertices: number[];
  faces: FaceData[];
}

export interface GraphData {
  guid?: string | undefined;
  name?: string | undefined;
  /** node keys in order (arbitrary hashable) */
  nodeKeys: AnyData[];
  /** column-wise, including x/y/z coordinates */
  nodeAttributes: AttributeColumn[];
  attributes: { [key: string]: AnyData };
  defaultNodeAttributes: { [key: string]: AnyData };
  defaultEdgeAttributes: { [key: string]: AnyData };
  /** Edges as index pairs into node_keys, with edge attributes stored column-wise. */
  edgeU: number[];
  edgeV: number[];
  edgeAttributes: AttributeColumn[];
}

export interface GraphData_AttributesEntry {
  key: string;
  value: AnyData | undefined;
}

export interface GraphData_DefaultNodeAttributesEntry {
  key: string;
  value: AnyData | undefined;
}

export interface GraphData_DefaultEdgeAttributesEntry {
  key: string;
  value: AnyData | undefined;
}

function createBaseAttributeColumn(): AttributeColumn {
  return {
    name: "",
    indices: [],
    kind: 0,
    doubles: [],
    ints: [],
    bools: [],
    values: [],
  };
}

export const AttributeColumn: MessageFns<AttributeColumn> = {
  encode(
    message: AttributeColumn,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    writer.uint32(18).fork();
    for (const v of message.indices) {
      writer.uint32(v);
    }
    writer.join();
    if (message.kind !== 0) {
      writer.uint32(24).uint32(message.kind);
    }
    writer.uint32(34).fork();
    for (const v of message.doubles) {
      writer.double(v);
    }
    writer.join();
    writer.uint32(42).fork();
    for (const v of message.ints) {
      writer.sint64(v);
    }
    writer.join();
    writer.uint32(50).fork();
    for (const v of message.bools) {
      writer.bool(v);
    }
    writer.join();
    for (const v of message.values) {
      AnyData.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttributeColumn {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttributeColumn();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.indices.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.indices.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.uint32();
          continue;
        }
        case 4: {
          if (tag === 33) {
            message.doubles.push(reader.double());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.doubles.push(reader.double());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag === 40) {
            message.ints.push(longToNumber(reader.sint64()));

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ints.push(longToNumber(reader.sint64()));
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag === 48) {
            message.bools.push(reader.bool());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.bools.push(reader.bool());
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.values.push(AnyData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttributeColumn {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      indices: globalThis.Array.isArray(object?.indices)
        ? object.indices.map((e: any) => globalThis.Number(e))
        : [],
      kind: isSet(object.kind) ? globalThis.Number(object.kind) : 0,
      doubles: globalThis.Array.isArray(object?.doubles)
        ? object.doubles.map((e: any) => globalThis.Number(e))
        : [],
      ints: globalThis.Array.isArray(object?.ints)
        ? object.ints.map((e: any) => globalThis.Number(e))
        : [],
      bools: globalThis.Array.isArray(object?.bools)
        ? object.bools.map((e: any) => globalThis.Boolean(e))
        : [],
      values: globalThis.Array.isArray(object?.values)
        ? object.values.map((e: any) => AnyData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AttributeColumn): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.indices?.length) {
      obj.indices = message.indices.map((e) => Math.round(e));
    }
    if (message.kind !== 0) {
      obj.kind = Math.round(message.kind);
    }
    if (message.doubles?.length) {
      obj.doubles = message.doubles;
    }
    if (message.ints?.length) {
      obj.ints = message.ints.map((e) => Math.round(e));
    }
    if (message.bools?.length) {
      obj.bools = message.bools;
    }
    if (message.values?.length) {
      obj.values = message.values.map((e) => AnyData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AttributeColumn>, I>>(
    base?: I,
  ): AttributeColumn {
    return AttributeColumn.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AttributeColumn>, I>>(
    object: I,
  ): AttributeColumn {
    const message = createBaseAttributeColumn();
    message.name = object.name ?? "";
    message.indices = object.indices?.map((e) => e) || [];
    message.kind = object.kind ?? 0;
    message.doubles = object.doubles?.map((e) => e) || [];
    message.ints = object.ints?.map((e) => e) || [];
    message.bools = object.bools?.map((e) => e) || [];
    message.values = object.values?.map((e) => AnyData.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMeshData(): MeshData {
  return {
    guid: undefined,
    name: undefined,
    vertices: [],
    faceVertices: [],
    faceSizes: [],
    attributes: {},
    vertexAttributeColumns: [],
    faceAttributeColumns: [],
    edgeAttributeColumns: [],
    edgeKeys: [],
    defaultVertexAttributes: {},
    defaultFaceAttributes: {},
    defaultEdgeAttributes: {},
  };
}

export const MeshData: MessageFns<MeshData> = {
  encode(
    message: MeshData,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.guid !== undefined) {
      writer.uint32(10).string(message.guid);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    writer.uint32(26).fork();
    for (const v of message.vertices) {
      writer.double(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.faceVertices) {
      writer.uint32(v);
    }
    writer.join();
    writer.uint32(98).fork();
    for (const v of message.faceSizes) {
      writer.uint32(v);
    }
    writer.join();
    globalThis.Object.entries(message.attributes).forEach(
      ([key, value]: [string, AnyData]) => {
        MeshData_AttributesEntry.encode(
          { key: key as any, value },
          writer.uint32(42).fork(),
        ).join();
      },
    );
    for (const v of message.vertexAttributeColumns) {
      AttributeColumn.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.faceAttributeColumns) {
      AttributeColumn.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.edgeAttributeColumns) {
      AttributeColumn.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.edgeKeys) {
      AnyData.encode(v!, writer.uint32(106).fork()).join();
    }
    globalThis.Object.entries(message.defaultVertexAttributes).forEach(
      ([key, value]: [string, AnyData]) => {
        MeshData_DefaultVertexAttributesEntry.encode(
          { key: key as any, value },
          writer.uint32(74).fork(),
        ).join();
      },
    );
    globalThis.Object.entries(message.defaultFaceAttributes).forEach(
      ([key, value]: [string, AnyData]) => {
        MeshData_DefaultFaceAttributesEntry.encode(
          { key: key as any, value },
          writer.uint32(82).fork(),
        ).join();
      },
    );
    globalThis.Object.entries(message.defaultEdgeAttributes).forEach(
      ([key, value]: [string, AnyData]) => {
        MeshData_DefaultEdgeAttributesEntry.encode(
          { key: key as any, value },
          writer.uint32(90).fork(),
        ).join();
      },
    );
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MeshData {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeshData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag === 25) {
            message.vertices.push(reader.double());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vertices.push(reader.double());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.faceVertices.push(reader.uint32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.faceVertices.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 12: {
          if (tag === 96) {
            message.faceSizes.push(reader.uint32());

            continue;
          }

          if (tag === 98) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.faceSizes.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = MeshData_AttributesEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry5.value !== undefined) {
            message.attributes[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.vertexAttributeColumns.push(
            AttributeColumn.decode(reader, reader.uint32()),
          );
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.faceAttributeColumns.push(
            AttributeColumn.decode(reader, reader.uint32()),
          );
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.edgeAttributeColumns.push(
            AttributeColumn.decode(reader, reader.uint32()),
          );
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.edgeKeys.push(AnyData.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          const entry9 = MeshData_DefaultVertexAttributesEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry9.value !== undefined) {
            message.defaultVertexAttributes[entry9.key] = entry9.value;
          }
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          const entry10 = MeshData_DefaultFaceAttributesEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry10.value !== undefined) {
            message.defaultFaceAttributes[entry10.key] = entry10.value;
          }
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          const entry11 = MeshData_DefaultEdgeAttributesEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry11.value !== undefined) {
            message.defaultEdgeAttributes[entry11.key] = entry11.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeshData {
    return {
      guid: isSet(object.guid) ? globalThis.String(object.guid) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      vertices: globalThis.Array.isArray(object?.vertices)
        ? object.vertices.map((e: any) => globalThis.Number(e))
        : [],
      faceVertices: globalThis.Array.isArray(object?.faceVertices)
        ? object.faceVertices.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.face_vertices)
          ? object.face_vertices.map((e: any) => globalThis.Number(e))
          : [],
      faceSizes: globalThis.Array.isArray(object?.faceSizes)
        ? object.faceSizes.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.face_sizes)
          ? object.face_sizes.map((e: any) => globalThis.Number(e))
          : [],
      attributes: isObject(object.attributes)
        ? (
            globalThis.Object.entries(object.attributes) as [string, any][]
          ).reduce(
            (acc: { [key: string]: AnyData }, [key, value]: [string, any]) => {
              acc[key] = AnyData.fromJSON(value);
              return acc;
            },
            {},
          )
        : {},
      vertexAttributeColumns: globalThis.Array.isArray(
        object?.vertexAttributeColumns,
      )
        ? object.vertexAttributeColumns.map((e: any) =>
            AttributeColumn.fromJSON(e),
          )
        : globalThis.Array.isArray(object?.vertex_attribute_columns)
          ? object.vertex_attribute_columns.map((e: any) =>
              AttributeColumn.fromJSON(e),
            )
          : [],
      faceAttributeColumns: globalThis.Array.isArray(
        object?.faceAttributeColumns,
      )
        ? object.faceAttributeColumns.map((e: any) =>
            AttributeColumn.fromJSON(e),
          )
        : globalThis.Array.isArray(object?.face_attribute_columns)
          ? object.face_attribute_columns.map((e: any) =>
              AttributeColumn.fromJSON(e),
            )
          : [],
      edgeAttributeColumns: globalThis.Array.isArray(
        object?.edgeAttributeColumns,
      )
        ? object.edgeAttributeColumns.map((e: any) =>
            AttributeColumn.fromJSON(e),
          )
        : globalThis.Array.isArray(object?.edge_attribute_columns)
          ? object.edge_attribute_columns.map((e: any) =>
              AttributeColumn.fromJSON(e),
            )
          : [],
      edgeKeys: globalThis.Array.isArray(object?.edgeKeys)
        ? object.edgeKeys.map((e: any) => AnyData.fromJSON(e))
        : globalThis.Array.isArray(object?.edge_keys)
          ? object.edge_keys.map((e: any) => AnyData.fromJSON(e))
          : [],
      defaultVertexAttributes: isObject(object.defaultVertexAttributes)
        ? (
            globalThis.Object.entries(object.defaultVertexAttributes) as [
              string,
              any,
            ][]
          ).reduce(
            (acc: { [key: string]: AnyData }, [key, value]: [string, any]) => {
              acc[key] = AnyData.fromJSON(value);
              return acc;
            },
            {},
          )
        : isObject(object.default_vertex_attributes)
          ? (
              globalThis.Object.entries(object.default_vertex_attributes) as [
                string,
                any,
              ][]
            ).reduce(
              (
                acc: { [key: string]: AnyData },
                [key, value]: [string, any],
              ) => {
                acc[key] = AnyData.fromJSON(value);
                return acc;
              },
              {},
            )
          : {},
      defaultFaceAttributes: isObject(object.defaultFaceAttributes)
        ? (
            globalThis.Object.entries(object.defaultFaceAttributes) as [
              string,
              any,
            ][]
          ).reduce(
            (acc: { [key: string]: AnyData }, [key, value]: [string, any]) => {
              acc[key] = AnyData.fromJSON(value);
              return acc;
            },
            {},
          )
        : isObject(object.default_face_attributes)
          ? (
              globalThis.Object.entries(object.default_face_attributes) as [
                string,
                any,
              ][]
            ).reduce(
              (
                acc: { [key: string]: AnyData },
                [key, value]: [string, any],
              ) => {
                acc[key] = AnyData.fromJSON(value);
                return acc;
              },
              {},
            )
          : {},
      defaultEdgeAttributes: isObject(object.defaultEdgeAttributes)
        ? (
            globalThis.Object.entries(object.defaultEdgeAttributes) as [
              string,
              any,
            ][]
          ).reduce(
            (acc: { [key: string]: AnyData }, [key, value]: [string, any]) => {
              acc[key] = AnyData.fromJSON(value);
              return acc;
            },
            {},
          )
        : isObject(object.default_edge_attributes)
          ? (
              globalThis.Object.entries(object.default_edge_attributes) as [
                string,
                any,
              ][]
            ).reduce(
              (
                acc: { [key: string]: AnyData },
                [key, value]: [string, any],
              ) => {
                acc[key] = AnyData.fromJSON(value);
                return acc;
              },
              {},
            )
          : {},
    };
  },

  toJSON(message: MeshData): unknown {
    const obj: any = {};
    if (message.guid !== undefined) {
      obj.guid = message.guid;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.vertices?.length) {
      obj.vertices = message.vertices;
    }
    if (message.faceVertices?.length) {
      obj.faceVertices = message.faceVertices.map((e) => Math.round(e));
    }
    if (message.faceSizes?.length) {
      obj.faceSizes = message.faceSizes.map((e) => Math.round(e));
    }
    if (message.attributes) {
      const entries = globalThis.Object.entries(message.attributes) as [
        string,
        AnyData,
      ][];
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = AnyData.toJSON(v);
        });
      }
    }
    if (message.vertexAttributeColumns?.length) {
      obj.vertexAttributeColumns = message.vertexAttributeColumns.map((e) =>
        AttributeColumn.toJSON(e),
      );
    }
    if (message.faceAttributeColumns?.length) {
      obj.faceAttributeColumns = message.faceAttributeColumns.map((e) =>
        AttributeColumn.toJSON(e),
      );
    }
    if (message.edgeAttributeColumns?.length) {
      obj.edgeAttributeColumns = message.edgeAttributeColumns.map((e) =>
        AttributeColumn.toJSON(e),
      );
    }
    if (message.edgeKeys?.length) {
      obj.edgeKeys = message.edgeKeys.map((e) => AnyData.toJSON(e));
    }
    if (message.defaultVertexAttributes) {
      const entries = globalThis.Object.entries(
        message.defaultVertexAttributes,
      ) as [string, AnyData][];
      if (entries.length > 0) {
        obj.defaultVertexAttributes = {};
        entries.forEach(([k, v]) => {
          obj.defaultVertexAttributes[k] = AnyData.toJSON(v);
        });
      }
    }
    if (message.defaultFaceAttributes) {
      const entries = globalThis.Object.entries(
        message.defaultFaceAttributes,
      ) as [string, AnyData][];
      if (entries.length > 0) {
        obj.defaultFaceAttributes = {};
        entries.forEach(([k, v]) => {
          obj.defaultFaceAttributes[k] = AnyData.toJSON(v);
        });
      }
    }
    if (message.defaultEdgeAttributes) {
      const entries = globalThis.Object.entries(
        message.defaultEdgeAttributes,
      ) as [string, AnyData][];
      if (entries.length > 0) {
        obj.defaultEdgeAttributes = {};
        entries.forEach(([k, v]) => {
          obj.defaultEdgeAttributes[k] = AnyData.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeshData>, I>>(base?: I): MeshData {
    return MeshData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeshData>, I>>(object: I): MeshData {
    const message = createBaseMeshData();
    message.guid = object.guid ?? undefined;
    message.name = object.name ?? undefined;
    message.vertices = object.vertices?.map((e) => e) || [];
    message.faceVertices = object.faceVertices?.map((e) => e) || [];
    message.faceSizes = object.faceSizes?.map((e) => e) || [];
    message.attributes = (
      globalThis.Object.entries(object.attributes ?? {}) as [string, AnyData][]
    ).reduce(
      (acc: { [key: string]: AnyData }, [key, value]: [string, AnyData]) => {
        if (value !== undefined) {
          acc[key] = AnyData.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.vertexAttributeColumns =
      object.vertexAttributeColumns?.map((e) =>
        AttributeColumn.fromPartial(e),
      ) || [];
    message.faceAttributeColumns =
      object.faceAttributeColumns?.map((e) => AttributeColumn.fromPartial(e)) ||
      [];
    message.edgeAttributeColumns =
      object.edgeAttributeColumns?.map((e) => AttributeColumn.fromPartial(e)) ||
      [];
    message.edgeKeys =
      object.edgeKeys?.map((e) => AnyData.fromPartial(e)) || [];
    message.defaultVertexAttributes = (
      globalThis.Object.entries(object.defaultVertexAttributes ?? {}) as [
        string,
        AnyData,
      ][]
    ).reduce(
      (acc: { [key: string]: AnyData }, [key, value]: [string, AnyData]) => {
        if (value !== undefined) {
          acc[key] = AnyData.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.defaultFaceAttributes = (
      globalThis.Object.entries(object.defaultFaceAttributes ?? {}) as [
        string,
        AnyData,
      ][]
    ).reduce(
      (acc: { [key: string]: AnyData }, [key, value]: [string, AnyData]) => {
        if (value !== undefined) {
          acc[key] = AnyData.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.defaultEdgeAttributes = (
      globalThis.Object.entries(object.defaultEdgeAttributes ?? {}) as [
        string,
        AnyData,
      ][]
    ).reduce(
      (acc: { [key: string]: AnyData }, [key, value]: [string, AnyData]) => {
        if (value !== undefined) {
          acc[key] = AnyData.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseMeshData_AttributesEntry(): MeshData_AttributesEntry {
  return { key: "", value: undefined };
}

export const MeshData_AttributesEntry: MessageFns<MeshData_AttributesEntry> = {
  encode(
    message: MeshData_AttributesEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      AnyData.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): MeshData_AttributesEntry {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeshData_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = AnyData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeshData_AttributesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: MeshData_AttributesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = AnyData.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeshData_AttributesEntry>, I>>(
    base?: I,
  ): MeshData_AttributesEntry {
    return MeshData_AttributesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeshData_AttributesEntry>, I>>(
    object: I,
  ): MeshData_AttributesEntry {
    const message = createBaseMeshData_AttributesEntry();
    message.key = object.key ?? "";
    message.value =
      object.value !== undefined && object.value !== null
        ? AnyData.fromPartial(object.value)
        : undefined;
    return message;
  },
};

function createBaseMeshData_DefaultVertexAttributesEntry(): MeshData_DefaultVertexAttributesEntry {
  return { key: "", value: undefined };
}

export const MeshData_DefaultVertexAttributesEntry: MessageFns<MeshData_DefaultVertexAttributesEntry> =
  {
    encode(
      message: MeshData_DefaultVertexAttributesEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== undefined) {
        AnyData.encode(message.value, writer.uint32(18).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): MeshData_DefaultVertexAttributesEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMeshData_DefaultVertexAttributesEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = AnyData.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): MeshData_DefaultVertexAttributesEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
      };
    },

    toJSON(message: MeshData_DefaultVertexAttributesEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== undefined) {
        obj.value = AnyData.toJSON(message.value);
      }
      return obj;
    },

    create<
      I extends Exact<DeepPartial<MeshData_DefaultVertexAttributesEntry>, I>,
    >(base?: I): MeshData_DefaultVertexAttributesEntry {
      return MeshData_DefaultVertexAttributesEntry.fromPartial(
        base ?? ({} as any),
      );
    },
    fromPartial<
      I extends Exact<DeepPartial<MeshData_DefaultVertexAttributesEntry>, I>,
    >(object: I): MeshData_DefaultVertexAttributesEntry {
      const message = createBaseMeshData_DefaultVertexAttributesEntry();
      message.key = object.key ?? "";
      message.value =
        object.value !== undefined && object.value !== null
          ? AnyData.fromPartial(object.value)
          : undefined;
      return message;
    },
  };

function createBaseMeshData_DefaultFaceAttributesEntry(): MeshData_DefaultFaceAttributesEntry {
  return { key: "", value: undefined };
}

export const MeshData_DefaultFaceAttributesEntry: MessageFns<MeshData_DefaultFaceAttributesEntry> =
  {
    encode(
      message: MeshData_DefaultFaceAttributesEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== undefined) {
        AnyData.encode(message.value, writer.uint32(18).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): MeshData_DefaultFaceAttributesEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMeshData_DefaultFaceAttributesEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = AnyData.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): MeshData_DefaultFaceAttributesEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
      };
    },

    toJSON(message: MeshData_DefaultFaceAttributesEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== undefined) {
        obj.value = AnyData.toJSON(message.value);
      }
      return obj;
    },

    create<
      I extends Exact<DeepPartial<MeshData_DefaultFaceAttributesEntry>, I>,
    >(base?: I): MeshData_DefaultFaceAttributesEntry {
      return MeshData_DefaultFaceAttributesEntry.fromPartial(
        base ?? ({} as any),
      );
    },
    fromPartial<
      I extends Exact<DeepPartial<MeshData_DefaultFaceAttributesEntry>, I>,
    >(object: I): MeshData_DefaultFaceAttributesEntry {
      const message = createBaseMeshData_DefaultFaceAttributesEntry();
      message.key = object.key ?? "";
      message.value =
        object.value !== undefined && object.value !== null
          ? AnyData.fromPartial(object.value)
          : undefined;
      return message;
    },
  };

function createBaseMeshData_DefaultEdgeAttributesEntry(): MeshData_DefaultEdgeAttributesEntry {
  return { key: "", value: undefined };
}

export const MeshData_DefaultEdgeAttributesEntry: MessageFns<MeshData_DefaultEdgeAttributesEntry> =
  {
    encode(
      message: MeshData_DefaultEdgeAttributesEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== undefined) {
        AnyData.encode(message.value, writer.uint32(18).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): MeshData_DefaultEdgeAttributesEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMeshData_DefaultEdgeAttributesEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = AnyData.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): MeshData_DefaultEdgeAttributesEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
      };
    },

    toJSON(message: MeshData_DefaultEdgeAttributesEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== undefined) {
        obj.value = AnyData.toJSON(message.value);
      }
      return obj;
    },

    create<
      I extends Exact<DeepPartial<MeshData_DefaultEdgeAttributesEntry>, I>,
    >(base?: I): MeshData_DefaultEdgeAttributesEntry {
      return MeshData_DefaultEdgeAttributesEntry.fromPartial(
        base ?? ({} as any),
      );
    },
    fromPartial<
      I extends Exact<DeepPartial<MeshData_DefaultEdgeAttributesEntry>, I>,
    >(object: I): MeshData_DefaultEdgeAttributesEntry {
      const message = createBaseMeshData_DefaultEdgeAttributesEntry();
      message.key = object.key ?? "";
      message.value =
        object.value !== undefined && object.value !== null
          ? AnyData.fromPartial(object.value)
          : undefined;
      return message;
    },
  };

function createBaseFaceData(): FaceData {
  return { vertexIndices: [] };
}

export const FaceData: MessageFns<FaceData> = {
  encode(
    message: FaceData,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.vertexIndices) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FaceData {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFaceData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.vertexIndices.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vertexIndices.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FaceData {
    return {
      vertexIndices: globalThis.Array.isArray(object?.vertexIndices)
        ? object.vertexIndices.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.vertex_indices)
          ? object.vertex_indices.map((e: any) => globalThis.Number(e))
          : [],
    };
  },

  toJSON(message: FaceData): unknown {
    const obj: any = {};
    if (message.vertexIndices?.length) {
      obj.vertexIndices = message.vertexIndices.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FaceData>, I>>(base?: I): FaceData {
    return FaceData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FaceData>, I>>(object: I): FaceData {
    const message = createBaseFaceData();
    message.vertexIndices = object.vertexIndices?.map((e) => e) || [];
    return message;
  },
};

function createBasePolyhedronData(): PolyhedronData {
  return { guid: undefined, name: undefined, vertices: [], faces: [] };
}

export const PolyhedronData: MessageFns<PolyhedronData> = {
  encode(
    message: PolyhedronData,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.guid !== undefined) {
      writer.uint32(10).string(message.guid);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    writer.uint32(26).fork();
    for (const v of message.vertices) {
      writer.double(v);
    }
    writer.join();
    for (const v of message.faces) {
      FaceData.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolyhedronData {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolyhedronData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag === 25) {
            message.vertices.push(reader.double());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vertices.push(reader.double());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.faces.push(FaceData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolyhedronData {
    return {
      guid: isSet(object.guid) ? globalThis.String(object.guid) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      vertices: globalThis.Array.isArray(object?.vertices)
        ? object.vertices.map((e: any) => globalThis.Number(e))
        : [],
      faces: globalThis.Array.isArray(object?.faces)
        ? object.faces.map((e: any) => FaceData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PolyhedronData): unknown {
    const obj: any = {};
    if (message.guid !== undefined) {
      obj.guid = message.guid;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.vertices?.length) {
      obj.vertices = message.vertices;
    }
    if (message.faces?.length) {
      obj.faces = message.faces.map((e) => FaceData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolyhedronData>, I>>(
    base?: I,
  ): PolyhedronData {
    return PolyhedronData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolyhedronData>, I>>(
    object: I,
  ): PolyhedronData {
    const message = createBasePolyhedronData();
    message.guid = object.guid ?? undefined;
    message.name = object.name ?? undefined;
    message.vertices = object.vertices?.map((e) => e) || [];
    message.faces = object.faces?.map((e) => FaceData.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGraphData(): GraphData {
  return {
    guid: undefined,
    name: undefined,
    nodeKeys: [],
    nodeAttributes: [],
    attributes: {},
    defaultNodeAttributes: {},
    defaultEdgeAttributes: {},
    edgeU: [],
    edgeV: [],
    edgeAttributes: [],
  };
}

export const GraphData: MessageFns<GraphData> = {
  encode(
    message: GraphData,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.guid !== undefined) {
      writer.uint32(10).string(message.guid);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.nodeKeys) {
      AnyData.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.nodeAttributes) {
      AttributeColumn.encode(v!, writer.uint32(34).fork()).join();
    }
    globalThis.Object.entries(message.attributes).forEach(
      ([key, value]: [string, AnyData]) => {
        GraphData_AttributesEntry.encode(
          { key: key as any, value },
          writer.uint32(42).fork(),
        ).join();
      },
    );
    globalThis.Object.entries(message.defaultNodeAttributes).forEach(
      ([key, value]: [string, AnyData]) => {
        GraphData_DefaultNodeAttributesEntry.encode(
          { key: key as any, value },
          writer.uint32(50).fork(),
        ).join();
      },
    );
    globalThis.Object.entries(message.defaultEdgeAttributes).forEach(
      ([key, value]: [string, AnyData]) => {
        GraphData_DefaultEdgeAttributesEntry.encode(
          { key: key as any, value },
          writer.uint32(58).fork(),
        ).join();
      },
    );
    writer.uint32(66).fork();
    for (const v of message.edgeU) {
      writer.uint32(v);
    }
    writer.join();
    writer.uint32(74).fork();
    for (const v of message.edgeV) {
      writer.uint32(v);
    }
    writer.join();
    for (const v of message.edgeAttributes) {
      AttributeColumn.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GraphData {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraphData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodeKeys.push(AnyData.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.nodeAttributes.push(
            AttributeColumn.decode(reader, reader.uint32()),
          );
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = GraphData_AttributesEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry5.value !== undefined) {
            message.attributes[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = GraphData_DefaultNodeAttributesEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry6.value !== undefined) {
            message.defaultNodeAttributes[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = GraphData_DefaultEdgeAttributesEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry7.value !== undefined) {
            message.defaultEdgeAttributes[entry7.key] = entry7.value;
          }
          continue;
        }
        case 8: {
          if (tag === 64) {
            message.edgeU.push(reader.uint32());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.edgeU.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 9: {
          if (tag === 72) {
            message.edgeV.push(reader.uint32());

            continue;
          }

          if (tag === 74) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.edgeV.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.edgeAttributes.push(
            AttributeColumn.decode(reader, reader.uint32()),
          );
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GraphData {
    return {
      guid: isSet(object.guid) ? globalThis.String(object.guid) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      nodeKeys: globalThis.Array.isArray(object?.nodeKeys)
        ? object.nodeKeys.map((e: any) => AnyData.fromJSON(e))
        : globalThis.Array.isArray(object?.node_keys)
          ? object.node_keys.map((e: any) => AnyData.fromJSON(e))
          : [],
      nodeAttributes: globalThis.Array.isArray(object?.nodeAttributes)
        ? object.nodeAttributes.map((e: any) => AttributeColumn.fromJSON(e))
        : globalThis.Array.isArray(object?.node_attributes)
          ? object.node_attributes.map((e: any) => AttributeColumn.fromJSON(e))
          : [],
      attributes: isObject(object.attributes)
        ? (
            globalThis.Object.entries(object.attributes) as [string, any][]
          ).reduce(
            (acc: { [key: string]: AnyData }, [key, value]: [string, any]) => {
              acc[key] = AnyData.fromJSON(value);
              return acc;
            },
            {},
          )
        : {},
      defaultNodeAttributes: isObject(object.defaultNodeAttributes)
        ? (
            globalThis.Object.entries(object.defaultNodeAttributes) as [
              string,
              any,
            ][]
          ).reduce(
            (acc: { [key: string]: AnyData }, [key, value]: [string, any]) => {
              acc[key] = AnyData.fromJSON(value);
              return acc;
            },
            {},
          )
        : isObject(object.default_node_attributes)
          ? (
              globalThis.Object.entries(object.default_node_attributes) as [
                string,
                any,
              ][]
            ).reduce(
              (
                acc: { [key: string]: AnyData },
                [key, value]: [string, any],
              ) => {
                acc[key] = AnyData.fromJSON(value);
                return acc;
              },
              {},
            )
          : {},
      defaultEdgeAttributes: isObject(object.defaultEdgeAttributes)
        ? (
            globalThis.Object.entries(object.defaultEdgeAttributes) as [
              string,
              any,
            ][]
          ).reduce(
            (acc: { [key: string]: AnyData }, [key, value]: [string, any]) => {
              acc[key] = AnyData.fromJSON(value);
              return acc;
            },
            {},
          )
        : isObject(object.default_edge_attributes)
          ? (
              globalThis.Object.entries(object.default_edge_attributes) as [
                string,
                any,
              ][]
            ).reduce(
              (
                acc: { [key: string]: AnyData },
                [key, value]: [string, any],
              ) => {
                acc[key] = AnyData.fromJSON(value);
                return acc;
              },
              {},
            )
          : {},
      edgeU: globalThis.Array.isArray(object?.edgeU)
        ? object.edgeU.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.edge_u)
          ? object.edge_u.map((e: any) => globalThis.Number(e))
          : [],
      edgeV: globalThis.Array.isArray(object?.edgeV)
        ? object.edgeV.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.edge_v)
          ? object.edge_v.map((e: any) => globalThis.Number(e))
          : [],
      edgeAttributes: globalThis.Array.isArray(object?.edgeAttributes)
        ? object.edgeAttributes.map((e: any) => AttributeColumn.fromJSON(e))
        : globalThis.Array.isArray(object?.edge_attributes)
          ? object.edge_attributes.map((e: any) => AttributeColumn.fromJSON(e))
          : [],
    };
  },

  toJSON(message: GraphData): unknown {
    const obj: any = {};
    if (message.guid !== undefined) {
      obj.guid = message.guid;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.nodeKeys?.length) {
      obj.nodeKeys = message.nodeKeys.map((e) => AnyData.toJSON(e));
    }
    if (message.nodeAttributes?.length) {
      obj.nodeAttributes = message.nodeAttributes.map((e) =>
        AttributeColumn.toJSON(e),
      );
    }
    if (message.attributes) {
      const entries = globalThis.Object.entries(message.attributes) as [
        string,
        AnyData,
      ][];
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = AnyData.toJSON(v);
        });
      }
    }
    if (message.defaultNodeAttributes) {
      const entries = globalThis.Object.entries(
        message.defaultNodeAttributes,
      ) as [string, AnyData][];
      if (entries.length > 0) {
        obj.defaultNodeAttributes = {};
        entries.forEach(([k, v]) => {
          obj.defaultNodeAttributes[k] = AnyData.toJSON(v);
        });
      }
    }
    if (message.defaultEdgeAttributes) {
      const entries = globalThis.Object.entries(
        message.defaultEdgeAttributes,
      ) as [string, AnyData][];
      if (entries.length > 0) {
        obj.defaultEdgeAttributes = {};
        entries.forEach(([k, v]) => {
          obj.defaultEdgeAttributes[k] = AnyData.toJSON(v);
        });
      }
    }
    if (message.edgeU?.length) {
      obj.edgeU = message.edgeU.map((e) => Math.round(e));
    }
    if (message.edgeV?.length) {
      obj.edgeV = message.edgeV.map((e) => Math.round(e));
    }
    if (message.edgeAttributes?.length) {
      obj.edgeAttributes = message.edgeAttributes.map((e) =>
        AttributeColumn.toJSON(e),
      );
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GraphData>, I>>(base?: I): GraphData {
    return GraphData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GraphData>, I>>(
    object: I,
  ): GraphData {
    const message = createBaseGraphData();
    message.guid = object.guid ?? undefined;
    message.name = object.name ?? undefined;
    message.nodeKeys =
      object.nodeKeys?.map((e) => AnyData.fromPartial(e)) || [];
    message.nodeAttributes =
      object.nodeAttributes?.map((e) => AttributeColumn.fromPartial(e)) || [];
    message.attributes = (
      globalThis.Object.entries(object.attributes ?? {}) as [string, AnyData][]
    ).reduce(
      (acc: { [key: string]: AnyData }, [key, value]: [string, AnyData]) => {
        if (value !== undefined) {
          acc[key] = AnyData.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.defaultNodeAttributes = (
      globalThis.Object.entries(object.defaultNodeAttributes ?? {}) as [
        string,
        AnyData,
      ][]
    ).reduce(
      (acc: { [key: string]: AnyData }, [key, value]: [string, AnyData]) => {
        if (value !== undefined) {
          acc[key] = AnyData.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.defaultEdgeAttributes = (
      globalThis.Object.entries(object.defaultEdgeAttributes ?? {}) as [
        string,
        AnyData,
      ][]
    ).reduce(
      (acc: { [key: string]: AnyData }, [key, value]: [string, AnyData]) => {
        if (value !== undefined) {
          acc[key] = AnyData.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.edgeU = object.edgeU?.map((e) => e) || [];
    message.edgeV = object.edgeV?.map((e) => e) || [];
    message.edgeAttributes =
      object.edgeAttributes?.map((e) => AttributeColumn.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGraphData_AttributesEntry(): GraphData_AttributesEntry {
  return { key: "", value: undefined };
}

export const GraphData_AttributesEntry: MessageFns<GraphData_AttributesEntry> =
  {
    encode(
      message: GraphData_AttributesEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== undefined) {
        AnyData.encode(message.value, writer.uint32(18).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): GraphData_AttributesEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGraphData_AttributesEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = AnyData.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): GraphData_AttributesEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
      };
    },

    toJSON(message: GraphData_AttributesEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== undefined) {
        obj.value = AnyData.toJSON(message.value);
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<GraphData_AttributesEntry>, I>>(
      base?: I,
    ): GraphData_AttributesEntry {
      return GraphData_AttributesEntry.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<GraphData_AttributesEntry>, I>>(
      object: I,
    ): GraphData_AttributesEntry {
      const message = createBaseGraphData_AttributesEntry();
      message.key = object.key ?? "";
      message.value =
        object.value !== undefined && object.value !== null
          ? AnyData.fromPartial(object.value)
          : undefined;
      return message;
    },
  };

function createBaseGraphData_DefaultNodeAttributesEntry(): GraphData_DefaultNodeAttributesEntry {
  return { key: "", value: undefined };
}

export const GraphData_DefaultNodeAttributesEntry: MessageFns<GraphData_DefaultNodeAttributesEntry> =
  {
    encode(
      message: GraphData_DefaultNodeAttributesEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== undefined) {
        AnyData.encode(message.value, writer.uint32(18).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): GraphData_DefaultNodeAttributesEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGraphData_DefaultNodeAttributesEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = AnyData.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): GraphData_DefaultNodeAttributesEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
      };
    },

    toJSON(message: GraphData_DefaultNodeAttributesEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== undefined) {
        obj.value = AnyData.toJSON(message.value);
      }
      return obj;
    },

    create<
      I extends Exact<DeepPartial<GraphData_DefaultNodeAttributesEntry>, I>,
    >(base?: I): GraphData_DefaultNodeAttributesEntry {
      return GraphData_DefaultNodeAttributesEntry.fromPartial(
        base ?? ({} as any),
      );
    },
    fromPartial<
      I extends Exact<DeepPartial<GraphData_DefaultNodeAttributesEntry>, I>,
    >(object: I): GraphData_DefaultNodeAttributesEntry {
      const message = createBaseGraphData_DefaultNodeAttributesEntry();
      message.key = object.key ?? "";
      message.value =
        object.value !== undefined && object.value !== null
          ? AnyData.fromPartial(object.value)
          : undefined;
      return message;
    },
  };

function createBaseGraphData_DefaultEdgeAttributesEntry(): GraphData_DefaultEdgeAttributesEntry {
  return { key: "", value: undefined };
}

export const GraphData_DefaultEdgeAttributesEntry: MessageFns<GraphData_DefaultEdgeAttributesEntry> =
  {
    encode(
      message: GraphData_DefaultEdgeAttributesEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== undefined) {
        AnyData.encode(message.value, writer.uint32(18).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): GraphData_DefaultEdgeAttributesEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGraphData_DefaultEdgeAttributesEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = AnyData.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): GraphData_DefaultEdgeAttributesEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
      };
    },

    toJSON(message: GraphData_DefaultEdgeAttributesEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== undefined) {
        obj.value = AnyData.toJSON(message.value);
      }
      return obj;
    },

    create<
      I extends Exact<DeepPartial<GraphData_DefaultEdgeAttributesEntry>, I>,
    >(base?: I): GraphData_DefaultEdgeAttributesEntry {
      return GraphData_DefaultEdgeAttributesEntry.fromPartial(
        base ?? ({} as any),
      );
    },
    fromPartial<
      I extends Exact<DeepPartial<GraphData_DefaultEdgeAttributesEntry>, I>,
    >(object: I): GraphData_DefaultEdgeAttributesEntry {
      const message = createBaseGraphData_DefaultEdgeAttributesEntry();
      message.key = object.key ?? "";
      message.value =
        object.value !== undefined && object.value !== null
          ? AnyData.fromPartial(object.value)
          : undefined;
      return message;
    },
  };

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
