// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.5
// source: compas_pb/data/datastructures.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PointData } from "./geometry";

export const protobufPackage = "compas_pb.data";

export interface FaceList {
  indices: number[];
}

export interface MeshData {
  guid?: string | undefined;
  name?: string | undefined;
  vertices: PointData[];
  faces: FaceList[];
}

export interface FaceData {
  /** indices into the vertices array */
  vertexIndices: number[];
}

export interface PolyhedronData {
  guid?: string | undefined;
  name?: string | undefined;
  vertices: PointData[];
  faces: FaceData[];
}

function createBaseFaceList(): FaceList {
  return { indices: [] };
}

export const FaceList: MessageFns<FaceList> = {
  encode(message: FaceList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.indices) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FaceList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFaceList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.indices.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.indices.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FaceList {
    return {
      indices: globalThis.Array.isArray(object?.indices) ? object.indices.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: FaceList): unknown {
    const obj: any = {};
    if (message.indices?.length) {
      obj.indices = message.indices.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FaceList>, I>>(base?: I): FaceList {
    return FaceList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FaceList>, I>>(object: I): FaceList {
    const message = createBaseFaceList();
    message.indices = object.indices?.map((e) => e) || [];
    return message;
  },
};

function createBaseMeshData(): MeshData {
  return { guid: undefined, name: undefined, vertices: [], faces: [] };
}

export const MeshData: MessageFns<MeshData> = {
  encode(message: MeshData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.guid !== undefined) {
      writer.uint32(10).string(message.guid);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.vertices) {
      PointData.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.faces) {
      FaceList.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MeshData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeshData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vertices.push(PointData.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.faces.push(FaceList.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeshData {
    return {
      guid: isSet(object.guid) ? globalThis.String(object.guid) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      vertices: globalThis.Array.isArray(object?.vertices)
        ? object.vertices.map((e: any) => PointData.fromJSON(e))
        : [],
      faces: globalThis.Array.isArray(object?.faces) ? object.faces.map((e: any) => FaceList.fromJSON(e)) : [],
    };
  },

  toJSON(message: MeshData): unknown {
    const obj: any = {};
    if (message.guid !== undefined) {
      obj.guid = message.guid;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.vertices?.length) {
      obj.vertices = message.vertices.map((e) => PointData.toJSON(e));
    }
    if (message.faces?.length) {
      obj.faces = message.faces.map((e) => FaceList.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeshData>, I>>(base?: I): MeshData {
    return MeshData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeshData>, I>>(object: I): MeshData {
    const message = createBaseMeshData();
    message.guid = object.guid ?? undefined;
    message.name = object.name ?? undefined;
    message.vertices = object.vertices?.map((e) => PointData.fromPartial(e)) || [];
    message.faces = object.faces?.map((e) => FaceList.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFaceData(): FaceData {
  return { vertexIndices: [] };
}

export const FaceData: MessageFns<FaceData> = {
  encode(message: FaceData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.vertexIndices) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FaceData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFaceData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.vertexIndices.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vertexIndices.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FaceData {
    return {
      vertexIndices: globalThis.Array.isArray(object?.vertexIndices)
        ? object.vertexIndices.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.vertex_indices)
        ? object.vertex_indices.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: FaceData): unknown {
    const obj: any = {};
    if (message.vertexIndices?.length) {
      obj.vertexIndices = message.vertexIndices.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FaceData>, I>>(base?: I): FaceData {
    return FaceData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FaceData>, I>>(object: I): FaceData {
    const message = createBaseFaceData();
    message.vertexIndices = object.vertexIndices?.map((e) => e) || [];
    return message;
  },
};

function createBasePolyhedronData(): PolyhedronData {
  return { guid: undefined, name: undefined, vertices: [], faces: [] };
}

export const PolyhedronData: MessageFns<PolyhedronData> = {
  encode(message: PolyhedronData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.guid !== undefined) {
      writer.uint32(10).string(message.guid);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.vertices) {
      PointData.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.faces) {
      FaceData.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolyhedronData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolyhedronData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vertices.push(PointData.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.faces.push(FaceData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolyhedronData {
    return {
      guid: isSet(object.guid) ? globalThis.String(object.guid) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      vertices: globalThis.Array.isArray(object?.vertices)
        ? object.vertices.map((e: any) => PointData.fromJSON(e))
        : [],
      faces: globalThis.Array.isArray(object?.faces) ? object.faces.map((e: any) => FaceData.fromJSON(e)) : [],
    };
  },

  toJSON(message: PolyhedronData): unknown {
    const obj: any = {};
    if (message.guid !== undefined) {
      obj.guid = message.guid;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.vertices?.length) {
      obj.vertices = message.vertices.map((e) => PointData.toJSON(e));
    }
    if (message.faces?.length) {
      obj.faces = message.faces.map((e) => FaceData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolyhedronData>, I>>(base?: I): PolyhedronData {
    return PolyhedronData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolyhedronData>, I>>(object: I): PolyhedronData {
    const message = createBasePolyhedronData();
    message.guid = object.guid ?? undefined;
    message.name = object.name ?? undefined;
    message.vertices = object.vertices?.map((e) => PointData.fromPartial(e)) || [];
    message.faces = object.faces?.map((e) => FaceData.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
